// how to get this set as a component?
// do we even need this as a component? we will need it on almost every page

import './Location.css';

function LocationAndHours() {
    return (
        <div className="LocationAndHours">
            hey
        </div>
    );
}

export default LocationAndHours();